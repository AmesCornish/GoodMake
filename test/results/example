+ ./example.sh all
GoodMake version X.X.X
Make src/list from ./example.sh because it hasn't completed
+ ls src
+ read file
+ echo tgt/input.txt
+ read file
+ echo tgt/list
+ read file
Make tgt/input.txt from ./example.sh because it hasn't completed
+ basename tgt/input.txt
+ SRC=src/input.txt
+ ./example.sh src/input.txt
GoodMake version X.X.X
Dependency src/input.txt
+ sort src/input.txt
Make tgt/list from ./example.sh because it hasn't completed
+ basename tgt/list
+ SRC=src/list
+ ./example.sh src/list
GoodMake version X.X.X
Skip src/list from ./example.sh because it was checked this build
+ sort src/list
Skip all from ./example.sh because dependencies unchanged
+ set +x
