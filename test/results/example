+ ./example.sh all
GoodMake version X.X.X
Make all from ./example.sh because it hasn't completed
+ SRC_DIR=src
+ ./example.sh tgt.ls
GoodMake version X.X.X
Make tgt.ls from ./example.sh because it hasn't completed
+ SRC_DIR=src
+ ls src
+ read file
+ echo tgt/input.txt
+ read file
+ cat tgt.ls
+ ./example.sh tgt/input.txt
GoodMake version X.X.X
Make tgt/input.txt from ./example.sh because it hasn't completed
+ SRC_DIR=src
+ basename tgt/input.txt
+ SRC=src/input.txt
+ ./example.sh src/input.txt
GoodMake version X.X.X
Dependency src/input.txt
+ sort src/input.txt
+ set +x
